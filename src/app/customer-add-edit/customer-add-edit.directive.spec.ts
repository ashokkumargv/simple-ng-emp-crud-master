import { CustomerAddEditDirective } from './customer-add-edit.directive';

describe('CustomerAddEditDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomerAddEditDirective();
    expect(directive).toBeTruthy();
  });
});
